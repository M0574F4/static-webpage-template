# Static Webpage Template

A minimal static webpage template for GitHub Pages.

Edit files in `public/`:

- `public/index.html`
- `public/styles.css`

Every push to `main` deploys the contents of `public/` to GitHub Pages through `.github/workflows/pages.yml`.

Read `SETUP.md` after creating a project from this template.
